/*package com.rendersoncs.reportform.viewHolder;

import android.content.Context;
import android.support.annotation.NonNull;
import android.support.v7.widget.RecyclerView;
import android.view.View;
import android.widget.TextView;

import com.rendersoncs.reportform.R;
import com.rendersoncs.reportform.itens.Repo;
import com.rendersoncs.reportform.listener.OnReportListenerInteractionListener;

public class ReportResumeListViewHolder extends RecyclerView.ViewHolder {

    private TextView title_list;
    private Context context;

    public ReportResumeListViewHolder(@NonNull View itemView, Context context) {
        super(itemView);

        this.title_list = itemView.findViewById(R.id.jo_title);
        this.context = context;
    }

    public void bindData(final Repo repoEntity){
        this.title_list.setText(repoEntity.getTitle_list());
    }

    public class ViewHolder {
    }
}
*/